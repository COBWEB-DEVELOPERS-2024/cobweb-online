import React from 'react';

const LearningSettingsView: React.FC = () => {
    return (
        <div className="w-full h-full flex flex-col">
            <p>Learning Settings (coming soon)</p>
        </div>
    );
}

export default LearningSettingsView;
