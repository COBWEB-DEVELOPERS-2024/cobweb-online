import React from 'react';

const WasteSettingsView: React.FC = () => {
    return (
        <div className="w-full h-full flex flex-col">
            <p>Waste Settings (coming soon)</p>
        </div>
    );
}

export default WasteSettingsView;
