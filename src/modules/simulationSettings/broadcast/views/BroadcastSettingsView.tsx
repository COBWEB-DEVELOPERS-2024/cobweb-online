import React from 'react';

const BroadcastSettingsView: React.FC = () => {
    return (
        <div className="w-full h-full flex flex-col">
            <p>Broadcast Settings (coming soon)</p>
        </div>
    );
}

export default BroadcastSettingsView;
