import React from 'react';

const GravitySettingsView: React.FC = () => {
    return (
        <div className="w-full h-full flex flex-col">
            <p>Gravity Settings (coming soon)</p>
        </div>
    );
}

export default GravitySettingsView;
