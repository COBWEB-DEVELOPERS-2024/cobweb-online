import React from 'react';

const VisionSettingsView: React.FC = () => {
    return (
        <div className="w-full h-full flex flex-col">
            <p>Vision Settings (coming soon)</p>
        </div>
    );
}

export default VisionSettingsView;
