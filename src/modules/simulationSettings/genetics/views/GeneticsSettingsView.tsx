import React from 'react';

const GeneticsSettingsView: React.FC = () => {
    return (
        <div className="w-full h-full flex flex-col">
            <p>Genetics Settings (coming soon)</p>
        </div>
    );
}

export default GeneticsSettingsView;
