import React from 'react';

const ProductionSettingsView: React.FC = () => {
    return (
        <div className="w-full h-full flex flex-col">
            <p>Production Settings (coming soon)</p>
        </div>
    );
}

export default ProductionSettingsView;
